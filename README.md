# Домашнее задание к работе 19

## Условие задачи
Разработайте программу, которая записывает в файл поля структуры задания 3 работы 18:
Вариант 3. Запись «Сотрудник»:
Фамилия
Имя
Отчество
Должность
Зарплата
Дата рождения

## 1. Алгоритм и блок-схема

### Алгоритм
1. **Начало**
2. Объявить структуру:
   - `Employee` 
3. Задать исходные данные:
   - `n` — количество сотрудников
4. Запустить цикл ввода данных (от i = 0 до n-1):
   - `emp[i].surname` = `Фамилия`
   - `emp[i].name` = `Имя`
   - `emp[i].patronymic` = `Отчество`
   - `emp[i].position` = `Должность`
   - `emp[i].salary` = `Зарплата`
   - `emp[i].day, emp[i].month, emp[i].year` = `Дата рождения (день, месяц, год)`
5. Сохранение названия файла:
   - `filename[100]` = `запись имени файла`
   - `fopen` = `открытие файла для записи в режиме «w»`
6. Записать в файл заголовок отчета и общее количество сотрудников.
7. Запустить цикл записи (от i = 0 до n-1).
8. Закрыть файл:
   - `fclose(file)`
10. Вывести сообщение об успешном сохранении.
11. **Конец**

### Блок-схема
<img width="761" height="827" alt="Диаграмма без названия drawio" src="https://github.com/user-attachments/assets/1eb95686-f543-4683-bf44-fd149d6657eb" />


## 2. Реализация программы

<#include <stdio.h>
#include <locale.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
    char surname[50];
    char name[50];
    char patronymic[50];
    char position[50];
    double salary;
    int day, month, year;
} Employee;

int main() {
	setlocale(LC_CTYPE, "RUS");
    system("chcp 1251");
    puts("\nЗапись данных сотрудника");
    printf("(Полная структура: ФИО, должность, зарплата, дата рождения)\n");
    int n;
    printf("\nВведите количество сотрудников ");
    scanf("%d", &n);
    if (n <= 0) {
        printf("Ошибка: нужно минимум 1 сотрудник\n");
        return 1;
    }

    Employee emp[50];

    if (n > 50) {
        printf("Запись ограничена! Запись 50 сотрудников:\n");
    }
    for (int i = 0; i < n; i++) {
        printf("\n--- Сотрудник №%d ---\n", i + 1);

        printf("Фамилия: ");
        scanf("%s", emp[i].surname);

        printf("Имя: ");
        scanf("%s", emp[i].name);

        printf("Отчество: ");
        scanf("%s", emp[i].patronymic);

        printf("Должность: ");
        scanf("%s", emp[i].position);

        printf("Зарплата: ");
        scanf("%lf", &emp[i].salary);

        printf("Дата рождения (день месяц год через пробел): ");
        scanf("%d %d %d", &emp[i].day, &emp[i].month, &emp[i].year);
    }

    
    char filename[100];
    printf("\nВведите имя файла для сохранения (например: employees.txt): ");
    scanf("%s", filename);

    FILE* file = fopen(filename, "w");
    if (file == NULL) {
        printf("Ошибка! Не могу создать файл '%s'\n", filename);
        return 1;
    }

    
    fprintf(file, "ОТЧЕТ ПО СОТРУДНИКАМ\n");
    fprintf(file, "====================\n");
    fprintf(file, "Всего сотрудников: %d\n\n", n);

    for (int i = 0; i < n; i++) {
        fprintf(file, "Сотрудник №%d\n", i + 1);
        fprintf(file, "  ФИО: %s %s %s\n", emp[i].surname, emp[i].name, emp[i].patronymic);
        fprintf(file, "  Должность: %s\n", emp[i].position);
        fprintf(file, "  Зарплата: %.2f\n", emp[i].salary);
        fprintf(file, "  Дата рождения: %02d.%02d.%04d\n\n",
            emp[i].day, emp[i].month, emp[i].year);
    }
    fclose(file);
    printf("\n!! - Данные успешно сохранены в файл '%s' - !!\n", filename);
    return 0;
}

>

## 3. Результаты работы программы
Текущая кодовая страница: 1251

Запись данных сотрудника
(Полная структура: ФИО, должность, зарплата, дата рождения)

Введите количество сотрудников 1

--- Сотрудник №1 ---
Фамилия: Крылов
Имя: Олег
Отчество: Андреевич
Должность: Директор
Зарплата: 99999
Дата рождения (день месяц год через пробел): 12 10 2007

Введите имя файла для сохранения (например: employees.txt): employees.txt

!! - Данные успешно сохранены в файл 'employees.txt' - !!

C:\Users\ASUS\Desktop\Arrays\19\x64\Debug\19.exe (процесс 17524) завершил работу с кодом 0 (0x0).
Чтобы автоматически закрывать консоль при остановке отладки, включите параметр "Сервис" ->"Параметры" ->"Отладка" -> "Автоматически закрыть консоль при остановке отладки".

## 4. Информация о разработчике
Чумачёк Владислав, бИЦ-252
